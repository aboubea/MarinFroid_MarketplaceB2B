{
  "buildCommand": "pnpm install --frozen-lockfile=false && pnpm db:generate && pnpm db:migrate && pnpm --filter @marin-froid/web build",
  "installCommand": "true",
  "outputDirectory": "apps/web/.next"
}
